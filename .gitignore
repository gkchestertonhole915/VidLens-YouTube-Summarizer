# —— 密钥/凭证（绝不上传）——
.env
cookies.txt
cookies.json
prompt.txt

# —— 本地数据 / 运行产物 ——
output/
*.zip

# —— Claude / 编辑器 / 缓存 ——
.claude/
.vscode/
.idea/
__pycache__/
*.pyc
*.pyo

# —— 第三方克隆（含 node_modules，用户自行 clone，见 README）——
bgutil-ytdlp-pot-provider/

# —— 系统文件 ——
.DS_Store
Thumbs.db
